// Exercise 1.2 — variable names must match exactly
//
// This program does not compile. There is a typo somewhere.
// Find it and fix it.

fn main() {
    let cipher_name = "Caesar";
    println!("We are building the {} cipher.", ciper_name);
}

// No assertions — if it compiles and prints the right thing, you've solved it.
