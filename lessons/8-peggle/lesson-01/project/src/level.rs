use macroquad::prelude::*;
use crate::peg::{Peg, PegKind};

// TODO: implement level_one() — return a Vec<Peg> with at least 20 pegs.
//   Arrange them in a grid. Make roughly 1-in-3 PegKind::Orange.
//   Hint: 5 rows × 9 columns, starting around x=100, y=180, spaced 75×70.
pub fn level_one() -> Vec<Peg> {
    todo!()
}
